{
  "$schema": "https://deno.land/x/denon/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-env --allow-read --allow-write --allow-plugin --unstable server.ts",
      "desc": "run my server.ts file"
    }
  }
}